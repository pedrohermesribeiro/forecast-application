server:
  port: ${PORT:8083}

spring:
  config:
    activate:
      on-profile: prod

# spring:
#   application:
#     name: user-service
#   datasource:
#     url: jdbc:h2:mem:forecast_user_db;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#     driver-class-name: org.h2.Driver
#     username: sa
#     password:
#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true
#     properties:
#       hibernate:
#         format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  security:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info

---
spring:
  config:
    activate:
      on-profile: prod
  h2:
    console:
      enabled: false


