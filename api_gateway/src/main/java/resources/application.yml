server:
  port: 8080  # O Gateway central ficará nessa porta

spring:
  cloud:
    gateway:
      routes:
        - id: auth
          uri: http://auth_service:8082
          predicates:
            - Path=/auth/**
        - id: users
          uri: http://user_service:8083
          predicates:
            - Path=/users/**
        - id: ai
          uri: http://ai_service:8081
          predicates:
            - Path=/ai/**
      globalcors:
        add-to-simple-url-handler-mapping: true
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:4200"
              - "https://forecast-app-dev-bjhxh3fjceeud7cv.brazilsouth-01.azurewebsites.net"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true

